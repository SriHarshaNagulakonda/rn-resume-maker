class Education {
    constructor(id, name, institution, place, score, start, end) {
      this.id = id;
      this.name = name;
      this.institution = institution;
      this.place = place;
      this.score = score;
      this.start = start;
      this.end = end;
    }
  }

export default Education;